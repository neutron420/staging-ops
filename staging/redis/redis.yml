apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-staging
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis-staging
  template:
    metadata:
      labels:
        app: redis-staging
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379
---
apiVersion: v1
kind: Service
metadata:
  name: redis-staging
spec:
  selector:
    app: redis-staging
  ports:
    - protocol: TCP
      port: 6379
      targetPort: 6379
  type: ClusterIP
