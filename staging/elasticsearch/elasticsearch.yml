apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: elasticsearch-staging
spec:
  serviceName: "elasticsearch-staging"
  replicas: 1
  selector:
    matchLabels:
      app: elasticsearch-staging
  template:
    metadata:
      labels:
        app: elasticsearch-staging
    spec:
      containers:
        - name: elasticsearch
          image: docker.elastic.co/elasticsearch/elasticsearch:8.11.1
          ports:
            - containerPort: 9200
          env:
            - name: discovery.type
              value: single-node
            - name: xpack.security.enabled
              value: "false"
            - name: ES_JAVA_OPTS
              value: "-Xms512m -Xmx512m"
          resources:
            requests:
              cpu: 200m
              memory: 1Gi
            limits:
              cpu: 1000m
              memory: 2Gi
          volumeMounts:
            - name: elastic-data
              mountPath: /usr/share/elasticsearch/data
  volumeClaimTemplates:
    - metadata:
        name: elastic-data
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: 10Gi
---
apiVersion: v1
kind: Service
metadata:
  name: elasticsearch-staging
spec:
  selector:
    app: elasticsearch-staging
  ports:
    - protocol: TCP
      port: 9200
      targetPort: 9200
  type: ClusterIP
